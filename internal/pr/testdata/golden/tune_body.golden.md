## noisefloor: tune TuneMe

Proposed change: fixture / TuneMe. Raise `for: 1m` to `for: 6m`.

p90 episode is 5m, current `for: 1m`; `for: 6m` would have suppressed 70% of past fires and retained 1 of 1 episodes longer than 12m.

This proposal is backed by ESTIMATED signals only: episode duration and silence history, inferred from firing shape. No pager enricher was configured for this scan, so no real acknowledgement data was available.

| metric | value |
| --- | --- |
| fires | 10 |
| confidence | 0.85 |
| observation window | 2026-08-15T00:00:00Z to 2026-09-14T00:00:00Z (30d) |

**How to check this**

Run the same range query against your own Prometheus, over the same window:

    ALERTS{alertname="TuneMe"}

For each episode, add the CURRENT `for: 1m` back onto its firing duration -- that is how long the underlying condition actually held, which is what a candidate `for:` is compared against (see the counterfactual sentence above). Count how many of the 10 episodes stay under the candidate `for: 6m`; that share is the suppressed rate stated above.

---
Generated by noisefloor from stored ALERTS history over the window above.
