## noisefloor: retire RetireMe

Proposed for deletion: fixture / RetireMe. This rule crosses noisefloor's retire threshold on the evidence below, gathered over the observation window stated here -- not asserted, checkable.

This proposal is backed by ESTIMATED signals only: episode duration and silence history, inferred from firing shape. No pager enricher was configured for this scan, so no real acknowledgement data was available.

| metric | value |
| --- | --- |
| fires | 120 |
| short-lived | 100% (shorter than 5m, i.e. resolved before anyone could act) |
| silenced | 40% |
| confidence | 0.85 |
| observation window | 2026-08-15T00:00:00Z to 2026-09-14T00:00:00Z (30d) |

**Silenced by:**

- alice, 2026-08-17T00:00:00Z to 2026-08-19T00:00:00Z: known flaky, ticket OPS-123

**How to check this**

Run the same range query against your own Prometheus, over the same window:

    ALERTS{alertname="RetireMe"}

Count episodes as one continuous run of `alertstate="firing"` samples on a single series, gap-tolerant to one query step. You should find approximately 120 episodes, 100% of them shorter than 5m.

That 5m figure is this rule's short-lived threshold: three times its `for: 0s`, clamped to the range 5m..30m.

---
Generated by noisefloor from stored ALERTS and Alertmanager silence history over the window above.
